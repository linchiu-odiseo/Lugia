import { Component } from '@angular/core';

// Placeholder para /tutor/exams/:recordId — la implementación completa llega
// en PR3 (TutorExamDetailViewModel + management screen).
// Esta shell permite que la ruta compile y que los guards funcionen
// en PR2 sin iniciar la lógica de gestión.
@Component({
  selector: 'app-tutor-exam-detail-page',
  template: '<p>Cargando gestión de examen…</p>',
})
export class TutorExamDetailPage {}
